type Job = {
    name: string,
    description: string,
    location: string,
    category: string,
    seniority: string,
    id: string
}

type Tag = {
    name: string,
    slug: string,
    id: string
}
